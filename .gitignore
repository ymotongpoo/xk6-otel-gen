.DS_Store
.amazonq/**
.claude/**
!.claude/settings.json
.codebuild/**
.worktrees/**
.vscode/**
.env
buildspec.yml
codebuild_build.sh
# Locally built k6 binary at repo root (xk6 build --output ./k6). Anchored so it
# does not also ignore the k6otelgen/ and k6output/ package directories.
/k6

# Codex CLI / Cursor implementation-agent run logs
logs/

# Local profiling artifacts (go test -cpuprofile etc.)
*.prof

# Ephemeral build/test scratch scripts
.tmp-*.sh
